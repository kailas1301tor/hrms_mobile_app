import 'package:flutter_riverpod/flutter_riverpod.dart';

// Import your notifiers here as you create them
// import 'package:sample_architecture_project/src/auth/notifier/login_form_notifier.dart';

disposeProviders(WidgetRef ref) {
  // ref.invalidate(homeNotifierProvider);
  // ref.invalidate(loginFormNotifierProvider);
}
