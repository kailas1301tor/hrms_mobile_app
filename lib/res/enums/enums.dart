enum LoaderState {
  loaded,
  loading,
  error,
  networkError,
  noData,
  serverError,
  noSearchData,
}

enum AuthStatus { authenticated, unauthenticated, loading }

enum UserRole { admin, user, guest }
